# =============================================================================
# RAILWAY STAGING CONFIGURATION
# =============================================================================
# This is a REFERENCE file. Actual values are set in Railway dashboard.
# Do NOT commit real secrets to this file.
# =============================================================================

# -----------------------------------------------------------------------------
# SIGNER (set in Railway secrets)
# -----------------------------------------------------------------------------
# Use a DIFFERENT key than production - staging attestations should be
# clearly distinguishable from production attestations.
SIGNER_PRIVATE_KEY=  # Set in Railway dashboard

# -----------------------------------------------------------------------------
# DATABASE (Railway Postgres addon)
# -----------------------------------------------------------------------------
# Railway provides DATABASE_URL automatically when you add Postgres
DATABASE_URL=  # Provided by Railway

# -----------------------------------------------------------------------------
# CHAIN
# -----------------------------------------------------------------------------
CHAIN_ID=84532  # Base Sepolia

# -----------------------------------------------------------------------------
# SCHEMA UIDs (registered on Base Sepolia)
# -----------------------------------------------------------------------------
# These are the default schema UIDs for policy attestations.
# Clients can override with custom schemas (e.g., for resolver contracts).
NUMERIC_SCHEMA_UID=0xc2b013ecb68d59b28f5d301203ec630335d97c37b400b16b359db6972572e02a
BOOLEAN_SCHEMA_UID=0x4958625091a773dcfb37a1c33099a378f32a975a7fb61f33d53c4be7589898f5
VERIFY_SCHEMA_UID=  # Register with: npx tsx scripts/register-schemas.ts

# -----------------------------------------------------------------------------
# SERVER
# -----------------------------------------------------------------------------
PORT=3000
NODE_ENV=production
ALLOWED_ORIGINS=https://staging.astral.global,http://localhost:3000
