[build]
dockerfilePath = "packages/compute-service/Dockerfile.staging"
watchPatterns = ["packages/compute-service/**", "package.json", "package-lock.json"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
